framework:
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    secret: "very secret indeed"
    test: ~

uma_schema:
    paths:
        - "%kernel.root_dir%/Resources/schema"
